@using SahinSoft.Domain.Enums
@model IReadOnlyList<SahinSoft.Domain.Entities.StockSlip>
@{
    ViewData["Title"] = "Stok Fişleri";
}

<div class="erp-toolbar">
    <div class="erp-toolbar-title">
        <h1>Stok Fişleri</h1>
        <span class="erp-toolbar-meta">@Model.Count kayıt</span>
    </div>
    <div class="erp-toolbar-actions">
        <a asp-action="Create" asp-route-type="@StockSlipType.Issue" class="btn btn-erp-outline btn-sm">+ Stok Çıkış Fişi</a>
        <a asp-action="Create" asp-route-type="@StockSlipType.Receipt" class="btn btn-erp-primary btn-sm">+ Stok Giriş Fişi</a>
    </div>
</div>

@if (TempData["Success"] is string success)
{
    <div class="alert alert-success py-2">@success</div>
}
@if (TempData["Error"] is string error)
{
    <div class="alert alert-danger py-2">@error</div>
}

<form method="get" class="erp-filter-bar">
    <div class="erp-field">
        <label>Tür</label>
        <select name="type" class="form-select" style="max-width: 160px;" asp-items="Html.GetEnumSelectList<StockSlipType>()">
            <option value="">Tüm türler</option>
        </select>
    </div>
    <div class="erp-field">
        <label>Durum</label>
        <select name="status" class="form-select" style="max-width: 160px;" asp-items="Html.GetEnumSelectList<StockSlipStatus>()">
            <option value="">Tüm durumlar</option>
        </select>
    </div>
    <div class="erp-field flex-grow-1">
        <label>Ara</label>
        <input class="form-control" autocomplete="off" placeholder="Fiş no ara..." data-live-search="#stockSlipsTable" />
    </div>
    <button class="btn btn-erp-primary">Filtrele</button>
</form>

<div class="erp-panel">
    <div class="erp-panel-body p-0">
    <div class="erp-table-wrap">
        <table class="erp-table" id="stockSlipsTable">
            <thead>
                <tr>
                    <th>Fiş No</th>
                    <th>Tür</th>
                    <th>Tarih</th>
                    <th>Depo</th>
                    <th>Durum</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="fw-semibold">@item.SlipNumber</td>
                    <td>@(item.SlipType == StockSlipType.Receipt ? "Giriş" : "Çıkış")</td>
                    <td>@item.SlipDateUtc.ToString("dd.MM.yyyy")</td>
                    <td>@item.Warehouse.Name</td>
                    <td>
                        @{
                            var badgeClass = item.Status switch
                            {
                                StockSlipStatus.Draft => "text-bg-secondary-subtle text-secondary-emphasis",
                                StockSlipStatus.Approved => "text-bg-success-subtle text-success-emphasis",
                                _ => "text-bg-danger-subtle text-danger-emphasis"
                            };
                            var statusText = item.Status switch
                            {
                                StockSlipStatus.Draft => "Taslak",
                                StockSlipStatus.Approved => "Onaylı",
                                _ => "İptal"
                            };
                        }
                        <span class="badge @badgeClass">@statusText</span>
                    </td>
                    <td class="text-end">
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-erp-outline" style="padding: 0.15rem 0.6rem; font-size: 0.78rem;">Görüntüle</a>
                    </td>
                </tr>
            }
            @if (Model.Count == 0)
            {
                <tr><td colspan="6" class="text-center text-secondary py-4">Kayıt bulunamadı.</td></tr>
            }
            </tbody>
        </table>
    </div>
    </div>
</div>
