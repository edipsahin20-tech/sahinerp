@model IReadOnlyList<SahinSoft.Domain.Entities.KitchenStation>
@{
    ViewData["Title"] = "Mutfak İstasyonları (Yazıcılar)";
}

<div class="erp-toolbar">
    <div class="erp-toolbar-title">
        <h1>Mutfak İstasyonları (Yazıcılar)</h1>
        <span class="erp-toolbar-meta">@Model.Count kayıt</span>
    </div>
    <div class="erp-toolbar-actions">
        <a asp-action="Create" class="btn btn-erp-primary btn-sm">+ Yeni İstasyon</a>
    </div>
</div>

@if (TempData["Success"] is string success)
{
    <div class="alert alert-success py-2">@success</div>
}
@if (TempData["Error"] is string error)
{
    <div class="alert alert-danger py-2">@error</div>
}

<div class="erp-panel">
    <div class="erp-panel-body p-0">
    <div class="erp-table-wrap">
        <table class="erp-table">
            <thead>
                <tr>
                    <th>Sıra</th>
                    <th>İstasyon Adı</th>
                    <th>Yazıcı Adı</th>
                    <th>Durum</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr class="@(item.IsActive ? "" : "text-secondary")">
                    <td>@item.DisplayOrder</td>
                    <td class="fw-semibold">@item.Name</td>
                    <td>@item.PrinterName</td>
                    <td>
                        @if (item.IsActive) { <span class="badge text-bg-success-subtle text-success-emphasis">Aktif</span> }
                        else { <span class="badge text-bg-danger-subtle text-danger-emphasis">Pasif</span> }
                    </td>
                    <td class="text-end">
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-erp-outline" style="padding: 0.15rem 0.6rem; font-size: 0.78rem;">Düzenle</a>
                    </td>
                </tr>
            }
            @if (Model.Count == 0)
            {
                <tr><td colspan="5" class="text-center text-secondary py-4">Kayıt bulunamadı. Örn: "Izgara", "Soğuk İçecek", "Adisyon" gibi istasyonlar tanımlayıp her birine bir yazıcı adı verebilirsiniz.</td></tr>
            }
            </tbody>
        </table>
    </div>
    </div>
</div>
