@using SahinSoft.Domain.Enums
@model IReadOnlyList<SahinSoft.Domain.Entities.StockTransfer>
@{
    ViewData["Title"] = "Depo Transferleri";
}

<div class="erp-toolbar">
    <div class="erp-toolbar-title">
        <h1>Depolar Arası Transfer</h1>
        <span class="erp-toolbar-meta">@Model.Count kayıt</span>
    </div>
    <div class="erp-toolbar-actions">
        <a asp-action="Create" class="btn btn-erp-primary btn-sm">+ Yeni Transfer</a>
    </div>
</div>

@if (TempData["Success"] is string success)
{
    <div class="alert alert-success py-2">@success</div>
}
@if (TempData["Error"] is string error)
{
    <div class="alert alert-danger py-2">@error</div>
}

<form method="get" class="erp-filter-bar">
    <div class="erp-field">
        <label>Durum</label>
        <select name="status" class="form-select" style="max-width: 160px;" asp-items="Html.GetEnumSelectList<StockTransferStatus>()">
            <option value="">Tüm durumlar</option>
        </select>
    </div>
    <div class="erp-field flex-grow-1">
        <label>Ara</label>
        <input class="form-control" autocomplete="off" placeholder="Transfer no ara..." data-live-search="#stockTransfersTable" />
    </div>
    <button class="btn btn-erp-primary">Filtrele</button>
</form>

<div class="erp-panel">
    <div class="erp-panel-body p-0">
    <div class="erp-table-wrap">
        <table class="erp-table" id="stockTransfersTable">
            <thead>
                <tr>
                    <th>Transfer No</th>
                    <th>Tarih</th>
                    <th>Çıkış Deposu</th>
                    <th>Giriş Deposu</th>
                    <th>Durum</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="fw-semibold">@item.TransferNumber</td>
                    <td>@item.TransferDateUtc.ToString("dd.MM.yyyy")</td>
                    <td>@item.FromWarehouse.Name</td>
                    <td>@item.ToWarehouse.Name</td>
                    <td>
                        @{
                            var badgeClass = item.Status switch
                            {
                                StockTransferStatus.Draft => "text-bg-secondary-subtle text-secondary-emphasis",
                                StockTransferStatus.Approved => "text-bg-success-subtle text-success-emphasis",
                                _ => "text-bg-danger-subtle text-danger-emphasis"
                            };
                            var statusText = item.Status switch
                            {
                                StockTransferStatus.Draft => "Taslak",
                                StockTransferStatus.Approved => "Onaylı",
                                _ => "İptal"
                            };
                        }
                        <span class="badge @badgeClass">@statusText</span>
                    </td>
                    <td class="text-end">
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-erp-outline" style="padding: 0.15rem 0.6rem; font-size: 0.78rem;">Görüntüle</a>
                    </td>
                </tr>
            }
            @if (Model.Count == 0)
            {
                <tr><td colspan="6" class="text-center text-secondary py-4">Kayıt bulunamadı.</td></tr>
            }
            </tbody>
        </table>
    </div>
    </div>
</div>
