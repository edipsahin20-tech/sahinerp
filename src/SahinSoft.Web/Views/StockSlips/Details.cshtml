@using SahinSoft.Domain.Enums
@model SahinSoft.Web.Models.StockSlipDetailsViewModel
@{
    ViewData["Title"] = $"Fiş {Model.SlipNumber}";
    var statusClass = Model.Status switch
    {
        StockSlipStatus.Draft => "erp-status-draft",
        StockSlipStatus.Approved => "erp-status-approved",
        _ => "erp-status-cancelled"
    };
    var statusText = Model.Status switch
    {
        StockSlipStatus.Draft => "Taslak",
        StockSlipStatus.Approved => "Onaylı",
        _ => "İptal"
    };
}

<div class="erp-toolbar">
    <div class="erp-toolbar-title">
        <h1>@(Model.SlipType == StockSlipType.Receipt ? "Stok Giriş Fişi" : "Stok Çıkış Fişi")</h1>
        <span class="erp-doc-no">@Model.SlipNumber</span>
        <span class="erp-status-badge @statusClass">@statusText</span>
    </div>
    <div class="erp-toolbar-actions">
        <a asp-action="Index" class="btn btn-erp-outline btn-sm">Liste</a>
        @if (Model.Status == StockSlipStatus.Draft)
        {
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-erp-outline btn-sm">Düzenle</a>
            <form method="post" asp-action="Approve" asp-route-id="@Model.Id" class="d-inline">
                <button type="submit" class="btn btn-erp-primary btn-sm">Onayla</button>
            </form>
        }
    </div>
</div>

@if (TempData["Success"] is string success)
{
    <div class="alert alert-success py-2">@success</div>
}
@if (TempData["Error"] is string error)
{
    <div class="alert alert-danger py-2">@error</div>
}

<div class="erp-panel">
    <div class="erp-panel-header">@Model.WarehouseName &middot; @Model.SlipDateUtc.ToString("dd.MM.yyyy")</div>
    <div class="erp-panel-body p-0">
        <div class="erp-table-wrap">
            <table class="erp-table">
                <thead>
                    <tr>
                        <th>Ürün</th>
                        <th class="text-end">Miktar</th>
                        <th class="text-end">Birim Maliyet</th>
                        <th>Açıklama</th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var line in Model.Lines)
                {
                    <tr>
                        <td>@line.ProductName</td>
                        <td class="text-end">@line.Quantity.ToString("N3")</td>
                        <td class="text-end">@line.UnitCost.ToString("N2")</td>
                        <td>@line.Description</td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
</div>

@if (Model.Status == StockSlipStatus.Approved && User.IsInRole(SahinSoft.Domain.Constants.AppRoles.Administrator))
{
    <div class="erp-panel">
        <div class="erp-panel-header">Fişi İptal Et</div>
        <div class="erp-panel-body">
            <p class="text-secondary small mb-2">İptal işlemi kaydı silmez; stok için ters hareket üretir.</p>
            <form method="post" asp-action="Cancel" asp-route-id="@Model.Id" class="d-flex gap-2">
                <input type="text" name="reason" class="form-control" placeholder="İptal gerekçesi" required />
                <button type="submit" class="btn btn-erp-danger text-nowrap">İptal Et</button>
            </form>
        </div>
    </div>
}
